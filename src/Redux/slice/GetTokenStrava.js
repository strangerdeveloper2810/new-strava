import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const GetTokenStrava = createSlice({
    name: "GetTokenStrava",
    initialState,
    reducers: {}
});


export default GetTokenStrava.reducer